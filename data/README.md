# Data

Input and output datasets used by the processing pipeline.

- `raw/` – Unprocessed article lists, wikitext, and reference data.
- `processed/` – Normalized citation data produced by the scripts.
- `alias_map.json` – Domain aliases used during URL normalization.
