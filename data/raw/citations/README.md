# [`/data/raw/citations`]

## Purpose

Raw Wikipedia citation datasets pulled in from an external archive for
exploration. These are bulk enwiki citation/heading dumps, not generated by the
project's own fetching scripts.

## Contents

### Committed files

* **[`featured-articles.csv`](./featured-articles.csv)** – Featured article list
  with `number, title, pageid, namespace, length, touched` columns.
* **[`good-articles.csv`](./good-articles.csv)** – Good article list, same
  columns as the featured list.
* **`flow_[e084a770]_enwiki_2016-06-01_CS1_citations_Flow.zip`** – Flow pipeline
  artifact bundle (a `.json5` flow definition plus an `artifact_*.data` blob).

### Untracked files (git-ignored — see [`/.gitignore`](../../../.gitignore))

The bulk `*.tsv` dumps total ~5.7 GB and are kept out of git history. Copy them
back from the archive if missing.

* **`wikipedia-citations_enwiki.tsv`** (~298 MB) – Citation identifiers per
  revision: `page_id, page_title, rev_id, timestamp, type, id` (e.g. `doi`).
* **`wikipedia-citations_enwiki 2.tsv`** (~393 MB) – Variant of the above.
* **`wikipedia-citations_enwiki_2016-06-01_CS1_citations.tsv`** (~3.5 GB) –
  Full CS1 citation records; final column is a JSON object per citation.
* **`wikipedia-citations_enwiki_20161101_headings.tsv`** (~1.1 GB) – Article
  section headings: `page_id, page_title, page_ns, heading_level, heading_text`.
* **`wikipedia-citations_page2cat.tsv`** (~363 MB) – Page-to-category mapping,
  one page per row with its categories tab-separated.

## Usage

Exploration data only — treat as read-only. The large TSVs are git-ignored, so
clones will not include them.

## Related

* [`../README.md`](../README.md) – Raw data overview
* [`../../processed/`](../../processed/) – Normalized outputs
