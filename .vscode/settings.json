{
  // Load the markdownlint-styleguide custom rules in the editor.
  // Rule configuration itself lives in .markdownlint-cli2.jsonc (the single
  // source of truth shared with the CLI and CI) and is discovered automatically.
  "markdownlint.customRules": [
    "markdownlint-styleguide"
  ]
}
